machine:
  php:
    version: 7.0.17

dependencies:
  override:
    - composer install --no-interaction
  cache_directories:
    - vendor

test:
  override:
    - composer --version
    - composer run test:coverage
  post:
    - bash <(curl -s https://codecov.io/bash) -t c9aad3ac-0438-4d28-9ee4-255cb6683823
